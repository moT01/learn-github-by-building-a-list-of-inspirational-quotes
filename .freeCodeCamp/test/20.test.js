/*const assert = require('assert');
const { getGithubUsername, fetchJson } = require('./utils');

describe('You', () => {
  it('should fork the suggested repo to your account', async () => {
    const githubUsername = await getGithubUsername();
    // const repo = await fetchJson(`https://api.github.com/repos/${githubUsername}/inspirational-quotes`);

    assert(true);
    //assert(repo.parent.html_url === 'https://github.com/mot03/inspirational-quotes');
  });
});*/
