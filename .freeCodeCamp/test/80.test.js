/*const assert = require('assert');
const { getLastCommand, getCwd } = require('./utils');

describe('You', () => {
  it('should view the branches in your repository with the correct flag', async () => {
    const lastCommand = await getLastCommand();
    const cwd = await getCwd();
    const lc2 = lastCommand[2] === '-a' || lastCommand[2] === '--all';

    assert(lastCommand[0] === 'git' && lastCommand[1] === 'branch' && lc2 && cwd === '/home/strove/project/inspirational-quotes');
  });
});*/
